const initState = {
    id:'',
    displayName:'',
    avatar:'',
    role:'',
    isLogin:true
}
export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
}